(function(g){"use strict";function line(canvas,series,opts={}){if(!canvas)return;const rect=canvas.getBoundingClientRect(),dpr=Math.max(1,window.devicePixelRatio||1),w=Math.max(320,Math.round(rect.width||700)),h=opts.height||300;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.height=h+'px';const c=canvas.getContext('2d');c.setTransform(dpr,0,0,dpr,0,0);c.clearRect(0,0,w,h);const pad={l:58,r:18,t:24,b:42},plotW=w-pad.l-pad.r,plotH=h-pad.t-pad.b;const all=series.flatMap(s=>s.data||[]),maxX=Math.max(1,...all.map(p=>+p.x||0)),maxY=Math.max(1,...all.map(p=>+p.y||0));c.font='12px system-ui';c.fillStyle='#64748b';c.strokeStyle='#e2e8f0';c.lineWidth=1;for(let i=0;i<=4;i++){const y=pad.t+plotH*i/4;c.beginPath();c.moveTo(pad.l,y);c.lineTo(w-pad.r,y);c.stroke();const val=maxY*(1-i/4);c.fillText(opts.money?compactMoney(val):Math.round(val).toLocaleString('en-IN'),5,y+4)}for(let i=0;i<=4;i++){const x=pad.l+plotW*i/4;c.fillText(Math.round(maxX*i/4).toString(),x-8,h-14)}c.fillText(opts.xLabel||'Month',w/2-18,h-4);const colors=['#155eef','#087a55','#c2410c','#7c3aed'];series.forEach((s,idx)=>{if(!s.data||!s.data.length)return;c.strokeStyle=s.color||colors[idx%colors.length];c.lineWidth=2.5;c.beginPath();s.data.forEach((p,j)=>{const x=pad.l+(+p.x||0)/maxX*plotW,y=pad.t+plotH-(+p.y||0)/maxY*plotH;j?c.lineTo(x,y):c.moveTo(x,y)});c.stroke()});const lx=pad.l;series.forEach((s,idx)=>{c.fillStyle=s.color||colors[idx%colors.length];c.fillRect(lx+idx*145,pad.t-17,12,3);c.fillStyle='#334155';c.fillText(s.name||('Series '+(idx+1)),lx+16+idx*145,pad.t-12)});}
function compactMoney(n){n=+n||0;if(n>=10000000)return'₹'+(n/10000000).toFixed(1)+'Cr';if(n>=100000)return'₹'+(n/100000).toFixed(1)+'L';if(n>=1000)return'₹'+Math.round(n/1000)+'k';return'₹'+Math.round(n)}
g.Charts={line};})(window);